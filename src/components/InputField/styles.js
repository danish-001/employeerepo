import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 6,
    padding: 10,
    margin: 10,
    fontSize: 18,
  },
  errorText: {
    color: 'crimson',
    fontWeight: 'bold',
    marginBottom: 4,
    marginTop: 2,
    textAlign: 'center',
  },
})

export default styles
